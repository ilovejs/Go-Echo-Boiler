// Code generated by SQLBoiler (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"database/sql"
	"fmt"
	"github.com/volatiletech/sqlboiler/types"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/volatiletech/null"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/strmangle"
)

// Claim is an object representing the database table.
type Claim struct {
	ID               int64             `boil:"id" json:"id" toml:"id" yaml:"id"`
	TradeItemID      int64             `boil:"trade_item_id" json:"trade_item_id" toml:"trade_item_id" yaml:"trade_item_id"`
	TotalAmount      types.NullDecimal `boil:"total_amount" json:"total_amount,omitempty" toml:"total_amount" yaml:"total_amount,omitempty"`
	ClaimedAmount    types.NullDecimal `boil:"ClaimedAmount" json:"ClaimedAmount,omitempty" toml:"ClaimedAmount" yaml:"ClaimedAmount,omitempty"`
	PreviousClaimed  types.NullDecimal `boil:"PreviousClaimed" json:"PreviousClaimed,omitempty" toml:"PreviousClaimed" yaml:"PreviousClaimed,omitempty"`
	AmountDue        types.NullDecimal `boil:"AmountDue" json:"AmountDue,omitempty" toml:"AmountDue" yaml:"AmountDue,omitempty"`
	CostToCompleted  types.NullDecimal `boil:"CostToCompleted" json:"CostToCompleted,omitempty" toml:"CostToCompleted" yaml:"CostToCompleted,omitempty"`
	IsDeleted        null.Bool         `boil:"IsDeleted" json:"IsDeleted,omitempty" toml:"IsDeleted" yaml:"IsDeleted,omitempty"`
	CreatedOn        null.Time         `boil:"CreatedOn" json:"CreatedOn,omitempty" toml:"CreatedOn" yaml:"CreatedOn,omitempty"`
	UpdatedOn        null.Time         `boil:"UpdatedOn" json:"UpdatedOn,omitempty" toml:"UpdatedOn" yaml:"UpdatedOn,omitempty"`
	LoginID          int64             `boil:"login_id" json:"login_id" toml:"login_id" yaml:"login_id"`
	ClaimNumber      null.String       `boil:"ClaimNumber" json:"ClaimNumber,omitempty" toml:"ClaimNumber" yaml:"ClaimNumber,omitempty"`
	ClaimPeriod      null.String       `boil:"ClaimPeriod" json:"ClaimPeriod,omitempty" toml:"ClaimPeriod" yaml:"ClaimPeriod,omitempty"`
	ActionClaim      null.Bool         `boil:"action_claim" json:"action_claim,omitempty" toml:"action_claim" yaml:"action_claim,omitempty"`
	AutoIncrement    null.Int64        `boil:"AutoIncrement" json:"AutoIncrement,omitempty" toml:"AutoIncrement" yaml:"AutoIncrement,omitempty"`
	OldClaimedAmount types.NullDecimal `boil:"old_claimed_amount" json:"old_claimed_amount,omitempty" toml:"old_claimed_amount" yaml:"old_claimed_amount,omitempty"`
	ClaimPercentage  types.NullDecimal `boil:"claim_percentage" json:"claim_percentage,omitempty" toml:"claim_percentage" yaml:"claim_percentage,omitempty"`
	TradeID          int               `boil:"trade_id" json:"trade_id" toml:"trade_id" yaml:"trade_id"`

	R *claimR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L claimL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var ClaimColumns = struct {
	ID               string
	TradeItemID      string
	TotalAmount      string
	ClaimedAmount    string
	PreviousClaimed  string
	AmountDue        string
	CostToCompleted  string
	IsDeleted        string
	CreatedOn        string
	UpdatedOn        string
	LoginID          string
	ClaimNumber      string
	ClaimPeriod      string
	ActionClaim      string
	AutoIncrement    string
	OldClaimedAmount string
	ClaimPercentage  string
	TradeID          string
}{
	ID:               "id",
	TradeItemID:      "trade_item_id",
	TotalAmount:      "total_amount",
	ClaimedAmount:    "ClaimedAmount",
	PreviousClaimed:  "PreviousClaimed",
	AmountDue:        "AmountDue",
	CostToCompleted:  "CostToCompleted",
	IsDeleted:        "IsDeleted",
	CreatedOn:        "CreatedOn",
	UpdatedOn:        "UpdatedOn",
	LoginID:          "login_id",
	ClaimNumber:      "ClaimNumber",
	ClaimPeriod:      "ClaimPeriod",
	ActionClaim:      "action_claim",
	AutoIncrement:    "AutoIncrement",
	OldClaimedAmount: "old_claimed_amount",
	ClaimPercentage:  "claim_percentage",
	TradeID:          "trade_id",
}

// Generated where

var ClaimWhere = struct {
	ID               whereHelperint64
	TradeItemID      whereHelperint64
	TotalAmount      whereHelpertypes_NullDecimal
	ClaimedAmount    whereHelpertypes_NullDecimal
	PreviousClaimed  whereHelpertypes_NullDecimal
	AmountDue        whereHelpertypes_NullDecimal
	CostToCompleted  whereHelpertypes_NullDecimal
	IsDeleted        whereHelpernull_Bool
	CreatedOn        whereHelpernull_Time
	UpdatedOn        whereHelpernull_Time
	LoginID          whereHelperint64
	ClaimNumber      whereHelpernull_String
	ClaimPeriod      whereHelpernull_String
	ActionClaim      whereHelpernull_Bool
	AutoIncrement    whereHelpernull_Int64
	OldClaimedAmount whereHelpertypes_NullDecimal
	ClaimPercentage  whereHelpertypes_NullDecimal
	TradeID          whereHelperint
}{
	ID:               whereHelperint64{field: "[dbo].[claims].[id]"},
	TradeItemID:      whereHelperint64{field: "[dbo].[claims].[trade_item_id]"},
	TotalAmount:      whereHelpertypes_NullDecimal{field: "[dbo].[claims].[total_amount]"},
	ClaimedAmount:    whereHelpertypes_NullDecimal{field: "[dbo].[claims].[ClaimedAmount]"},
	PreviousClaimed:  whereHelpertypes_NullDecimal{field: "[dbo].[claims].[PreviousClaimed]"},
	AmountDue:        whereHelpertypes_NullDecimal{field: "[dbo].[claims].[AmountDue]"},
	CostToCompleted:  whereHelpertypes_NullDecimal{field: "[dbo].[claims].[CostToCompleted]"},
	IsDeleted:        whereHelpernull_Bool{field: "[dbo].[claims].[IsDeleted]"},
	CreatedOn:        whereHelpernull_Time{field: "[dbo].[claims].[CreatedOn]"},
	UpdatedOn:        whereHelpernull_Time{field: "[dbo].[claims].[UpdatedOn]"},
	LoginID:          whereHelperint64{field: "[dbo].[claims].[login_id]"},
	ClaimNumber:      whereHelpernull_String{field: "[dbo].[claims].[ClaimNumber]"},
	ClaimPeriod:      whereHelpernull_String{field: "[dbo].[claims].[ClaimPeriod]"},
	ActionClaim:      whereHelpernull_Bool{field: "[dbo].[claims].[action_claim]"},
	AutoIncrement:    whereHelpernull_Int64{field: "[dbo].[claims].[AutoIncrement]"},
	OldClaimedAmount: whereHelpertypes_NullDecimal{field: "[dbo].[claims].[old_claimed_amount]"},
	ClaimPercentage:  whereHelpertypes_NullDecimal{field: "[dbo].[claims].[claim_percentage]"},
	TradeID:          whereHelperint{field: "[dbo].[claims].[trade_id]"},
}

// ClaimRels is where relationship names are stored.
var ClaimRels = struct {
	TradeItem      string
	Login          string
	Trade          string
	ClaimHistories string
}{
	TradeItem:      "TradeItem",
	Login:          "Login",
	Trade:          "Trade",
	ClaimHistories: "ClaimHistories",
}

// claimR is where relationships are stored.
type claimR struct {
	TradeItem      *TradeItem
	Login          *Profile
	Trade          *BasicTradeitem
	ClaimHistories ClaimHistorySlice
}

// NewStruct creates a new relationship struct
func (*claimR) NewStruct() *claimR {
	return &claimR{}
}

// claimL is where Load methods for each relationship are stored.
type claimL struct{}

var (
	claimAllColumns            = []string{"id", "trade_item_id", "total_amount", "ClaimedAmount", "PreviousClaimed", "AmountDue", "CostToCompleted", "IsDeleted", "CreatedOn", "UpdatedOn", "login_id", "ClaimNumber", "ClaimPeriod", "action_claim", "AutoIncrement", "old_claimed_amount", "claim_percentage", "trade_id"}
	claimColumnsWithAuto       = []string{}
	claimColumnsWithoutDefault = []string{"trade_item_id", "total_amount", "ClaimedAmount", "PreviousClaimed", "AmountDue", "CostToCompleted", "IsDeleted", "CreatedOn", "UpdatedOn", "login_id", "ClaimNumber", "ClaimPeriod", "action_claim", "AutoIncrement", "old_claimed_amount", "claim_percentage", "trade_id"}
	claimColumnsWithDefault    = []string{"id"}
	claimPrimaryKeyColumns     = []string{"id"}
)

type (
	// ClaimSlice is an alias for a slice of pointers to Claim.
	// This should generally be used opposed to []Claim.
	ClaimSlice []*Claim

	claimQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	claimType                 = reflect.TypeOf(&Claim{})
	claimMapping              = queries.MakeStructMapping(claimType)
	claimPrimaryKeyMapping, _ = queries.BindMapping(claimType, claimMapping, claimPrimaryKeyColumns)
	claimInsertCacheMut       sync.RWMutex
	claimInsertCache          = make(map[string]insertCache)
	claimUpdateCacheMut       sync.RWMutex
	claimUpdateCache          = make(map[string]updateCache)
	claimUpsertCacheMut       sync.RWMutex
	claimUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single claim record from the query.
func (q claimQuery) One(exec boil.Executor) (*Claim, error) {
	o := &Claim{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(nil, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for claims")
	}

	return o, nil
}

// All returns all Claim records from the query.
func (q claimQuery) All(exec boil.Executor) (ClaimSlice, error) {
	var o []*Claim

	err := q.Bind(nil, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to Claim slice")
	}

	return o, nil
}

// Count returns the count of all Claim records in the query.
func (q claimQuery) Count(exec boil.Executor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count claims rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q claimQuery) Exists(exec boil.Executor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if claims exists")
	}

	return count > 0, nil
}

// TradeItem pointed to by the foreign key.
func (o *Claim) TradeItem(mods ...qm.QueryMod) tradeItemQuery {
	queryMods := []qm.QueryMod{
		qm.Where("id=?", o.TradeItemID),
	}

	queryMods = append(queryMods, mods...)

	query := TradeItems(queryMods...)
	queries.SetFrom(query.Query, "[dbo].[trade_items]")

	return query
}

// Login pointed to by the foreign key.
func (o *Claim) Login(mods ...qm.QueryMod) profileQuery {
	queryMods := []qm.QueryMod{
		qm.Where("id=?", o.LoginID),
	}

	queryMods = append(queryMods, mods...)

	query := Profiles(queryMods...)
	queries.SetFrom(query.Query, "[dbo].[profiles]")

	return query
}

// Trade pointed to by the foreign key.
func (o *Claim) Trade(mods ...qm.QueryMod) basicTradeitemQuery {
	queryMods := []qm.QueryMod{
		qm.Where("id=?", o.TradeID),
	}

	queryMods = append(queryMods, mods...)

	query := BasicTradeitems(queryMods...)
	queries.SetFrom(query.Query, "[dbo].[basic_tradeitems]")

	return query
}

// ClaimHistories retrieves all the claim_history's ClaimHistories with an executor.
func (o *Claim) ClaimHistories(mods ...qm.QueryMod) claimHistoryQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("[dbo].[claim_histories].[claim_id]=?", o.ID),
	)

	query := ClaimHistories(queryMods...)
	queries.SetFrom(query.Query, "[dbo].[claim_histories]")

	if len(queries.GetSelect(query.Query)) == 0 {
		queries.SetSelect(query.Query, []string{"[dbo].[claim_histories].*"})
	}

	return query
}

// LoadTradeItem allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (claimL) LoadTradeItem(e boil.Executor, singular bool, maybeClaim interface{}, mods queries.Applicator) error {
	var slice []*Claim
	var object *Claim

	if singular {
		object = maybeClaim.(*Claim)
	} else {
		slice = *maybeClaim.(*[]*Claim)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &claimR{}
		}
		args = append(args, object.TradeItemID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &claimR{}
			}

			for _, a := range args {
				if a == obj.TradeItemID {
					continue Outer
				}
			}

			args = append(args, obj.TradeItemID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(qm.From(`dbo.trade_items`), qm.WhereIn(`id in ?`, args...))
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load TradeItem")
	}

	var resultSlice []*TradeItem
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice TradeItem")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for trade_items")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for trade_items")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.TradeItem = foreign
		if foreign.R == nil {
			foreign.R = &tradeItemR{}
		}
		foreign.R.Claims = append(foreign.R.Claims, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.TradeItemID == foreign.ID {
				local.R.TradeItem = foreign
				if foreign.R == nil {
					foreign.R = &tradeItemR{}
				}
				foreign.R.Claims = append(foreign.R.Claims, local)
				break
			}
		}
	}

	return nil
}

// LoadLogin allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (claimL) LoadLogin(e boil.Executor, singular bool, maybeClaim interface{}, mods queries.Applicator) error {
	var slice []*Claim
	var object *Claim

	if singular {
		object = maybeClaim.(*Claim)
	} else {
		slice = *maybeClaim.(*[]*Claim)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &claimR{}
		}
		args = append(args, object.LoginID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &claimR{}
			}

			for _, a := range args {
				if a == obj.LoginID {
					continue Outer
				}
			}

			args = append(args, obj.LoginID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(qm.From(`dbo.profiles`), qm.WhereIn(`id in ?`, args...))
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Profile")
	}

	var resultSlice []*Profile
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Profile")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for profiles")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for profiles")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Login = foreign
		if foreign.R == nil {
			foreign.R = &profileR{}
		}
		foreign.R.LoginClaims = append(foreign.R.LoginClaims, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.LoginID == foreign.ID {
				local.R.Login = foreign
				if foreign.R == nil {
					foreign.R = &profileR{}
				}
				foreign.R.LoginClaims = append(foreign.R.LoginClaims, local)
				break
			}
		}
	}

	return nil
}

// LoadTrade allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (claimL) LoadTrade(e boil.Executor, singular bool, maybeClaim interface{}, mods queries.Applicator) error {
	var slice []*Claim
	var object *Claim

	if singular {
		object = maybeClaim.(*Claim)
	} else {
		slice = *maybeClaim.(*[]*Claim)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &claimR{}
		}
		args = append(args, object.TradeID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &claimR{}
			}

			for _, a := range args {
				if a == obj.TradeID {
					continue Outer
				}
			}

			args = append(args, obj.TradeID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(qm.From(`dbo.basic_tradeitems`), qm.WhereIn(`id in ?`, args...))
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load BasicTradeitem")
	}

	var resultSlice []*BasicTradeitem
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice BasicTradeitem")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for basic_tradeitems")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for basic_tradeitems")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Trade = foreign
		if foreign.R == nil {
			foreign.R = &basicTradeitemR{}
		}
		foreign.R.TradeClaims = append(foreign.R.TradeClaims, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.TradeID == foreign.ID {
				local.R.Trade = foreign
				if foreign.R == nil {
					foreign.R = &basicTradeitemR{}
				}
				foreign.R.TradeClaims = append(foreign.R.TradeClaims, local)
				break
			}
		}
	}

	return nil
}

// LoadClaimHistories allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (claimL) LoadClaimHistories(e boil.Executor, singular bool, maybeClaim interface{}, mods queries.Applicator) error {
	var slice []*Claim
	var object *Claim

	if singular {
		object = maybeClaim.(*Claim)
	} else {
		slice = *maybeClaim.(*[]*Claim)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &claimR{}
		}
		args = append(args, object.ID)
	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &claimR{}
			}

			for _, a := range args {
				if a == obj.ID {
					continue Outer
				}
			}

			args = append(args, obj.ID)
		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(qm.From(`dbo.claim_histories`), qm.WhereIn(`claim_id in ?`, args...))
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load claim_histories")
	}

	var resultSlice []*ClaimHistory
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice claim_histories")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on claim_histories")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for claim_histories")
	}

	if singular {
		object.R.ClaimHistories = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &claimHistoryR{}
			}
			foreign.R.Claim = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.ClaimID {
				local.R.ClaimHistories = append(local.R.ClaimHistories, foreign)
				if foreign.R == nil {
					foreign.R = &claimHistoryR{}
				}
				foreign.R.Claim = local
				break
			}
		}
	}

	return nil
}

// SetTradeItem of the claim to the related item.
// Sets o.R.TradeItem to related.
// Adds o to related.R.Claims.
func (o *Claim) SetTradeItem(exec boil.Executor, insert bool, related *TradeItem) error {
	var err error
	if insert {
		if err = related.Insert(exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE [dbo].[claims] SET %s WHERE %s",
		strmangle.SetParamNames("[", "]", 1, []string{"trade_item_id"}),
		strmangle.WhereClause("[", "]", 2, claimPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.TradeItemID = related.ID
	if o.R == nil {
		o.R = &claimR{
			TradeItem: related,
		}
	} else {
		o.R.TradeItem = related
	}

	if related.R == nil {
		related.R = &tradeItemR{
			Claims: ClaimSlice{o},
		}
	} else {
		related.R.Claims = append(related.R.Claims, o)
	}

	return nil
}

// SetLogin of the claim to the related item.
// Sets o.R.Login to related.
// Adds o to related.R.LoginClaims.
func (o *Claim) SetLogin(exec boil.Executor, insert bool, related *Profile) error {
	var err error
	if insert {
		if err = related.Insert(exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE [dbo].[claims] SET %s WHERE %s",
		strmangle.SetParamNames("[", "]", 1, []string{"login_id"}),
		strmangle.WhereClause("[", "]", 2, claimPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.LoginID = related.ID
	if o.R == nil {
		o.R = &claimR{
			Login: related,
		}
	} else {
		o.R.Login = related
	}

	if related.R == nil {
		related.R = &profileR{
			LoginClaims: ClaimSlice{o},
		}
	} else {
		related.R.LoginClaims = append(related.R.LoginClaims, o)
	}

	return nil
}

// SetTrade of the claim to the related item.
// Sets o.R.Trade to related.
// Adds o to related.R.TradeClaims.
func (o *Claim) SetTrade(exec boil.Executor, insert bool, related *BasicTradeitem) error {
	var err error
	if insert {
		if err = related.Insert(exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE [dbo].[claims] SET %s WHERE %s",
		strmangle.SetParamNames("[", "]", 1, []string{"trade_id"}),
		strmangle.WhereClause("[", "]", 2, claimPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.TradeID = related.ID
	if o.R == nil {
		o.R = &claimR{
			Trade: related,
		}
	} else {
		o.R.Trade = related
	}

	if related.R == nil {
		related.R = &basicTradeitemR{
			TradeClaims: ClaimSlice{o},
		}
	} else {
		related.R.TradeClaims = append(related.R.TradeClaims, o)
	}

	return nil
}

// AddClaimHistories adds the given related objects to the existing relationships
// of the claim, optionally inserting them as new records.
// Appends related to o.R.ClaimHistories.
// Sets related.R.Claim appropriately.
func (o *Claim) AddClaimHistories(exec boil.Executor, insert bool, related ...*ClaimHistory) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.ClaimID = o.ID
			if err = rel.Insert(exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE [dbo].[claim_histories] SET %s WHERE %s",
				strmangle.SetParamNames("[", "]", 1, []string{"claim_id"}),
				strmangle.WhereClause("[", "]", 2, claimHistoryPrimaryKeyColumns),
			)
			values := []interface{}{o.ID, rel.ID}

			if boil.DebugMode {
				fmt.Fprintln(boil.DebugWriter, updateQuery)
				fmt.Fprintln(boil.DebugWriter, values)
			}

			if _, err = exec.Exec(updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.ClaimID = o.ID
		}
	}

	if o.R == nil {
		o.R = &claimR{
			ClaimHistories: related,
		}
	} else {
		o.R.ClaimHistories = append(o.R.ClaimHistories, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &claimHistoryR{
				Claim: o,
			}
		} else {
			rel.R.Claim = o
		}
	}
	return nil
}

// Claims retrieves all the records using an executor.
func Claims(mods ...qm.QueryMod) claimQuery {
	mods = append(mods, qm.From("[dbo].[claims]"))
	return claimQuery{NewQuery(mods...)}
}

// FindClaim retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindClaim(exec boil.Executor, iD int64, selectCols ...string) (*Claim, error) {
	claimObj := &Claim{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from [dbo].[claims] where [id]=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(nil, exec, claimObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from claims")
	}

	return claimObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *Claim) Insert(exec boil.Executor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no claims provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(claimColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	claimInsertCacheMut.RLock()
	cache, cached := claimInsertCache[key]
	claimInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			claimAllColumns,
			claimColumnsWithDefault,
			claimColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(claimType, claimMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(claimType, claimMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO [dbo].[claims] ([%s]) %%sVALUES (%s)%%s", strings.Join(wl, "],["), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO [dbo].[claims] %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryOutput = fmt.Sprintf("OUTPUT INSERTED.[%s] ", strings.Join(returnColumns, "],INSERTED.["))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into claims")
	}

	if !cached {
		claimInsertCacheMut.Lock()
		claimInsertCache[key] = cache
		claimInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the Claim.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *Claim) Update(exec boil.Executor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	claimUpdateCacheMut.RLock()
	cache, cached := claimUpdateCache[key]
	claimUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			claimAllColumns,
			claimPrimaryKeyColumns,
		)
		wl = strmangle.SetComplement(wl, claimColumnsWithAuto)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update claims, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE [dbo].[claims] SET %s WHERE %s",
			strmangle.SetParamNames("[", "]", 1, wl),
			strmangle.WhereClause("[", "]", len(wl)+1, claimPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(claimType, claimMapping, append(wl, claimPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	var result sql.Result
	result, err = exec.Exec(cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update claims row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for claims")
	}

	if !cached {
		claimUpdateCacheMut.Lock()
		claimUpdateCache[key] = cache
		claimUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q claimQuery) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for claims")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for claims")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o ClaimSlice) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), claimPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE [dbo].[claims] SET %s WHERE %s",
		strmangle.SetParamNames("[", "]", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, claimPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in claim slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all claim")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
func (o *Claim) Upsert(exec boil.Executor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no claims provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(claimColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	claimUpsertCacheMut.RLock()
	cache, cached := claimUpsertCache[key]
	claimUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			claimAllColumns,
			claimColumnsWithDefault,
			claimColumnsWithoutDefault,
			nzDefaults,
		)
		insert = strmangle.SetComplement(insert, claimColumnsWithAuto)
		for i, v := range insert {
			if strmangle.ContainsAny(claimPrimaryKeyColumns, v) && strmangle.ContainsAny(claimColumnsWithDefault, v) {
				insert = append(insert[:i], insert[i+1:]...)
			}
		}
		if len(insert) == 0 {
			return errors.New("models: unable to upsert claims, could not build insert column list")
		}

		ret = strmangle.SetMerge(ret, claimColumnsWithAuto)
		ret = strmangle.SetMerge(ret, claimColumnsWithDefault)

		update := updateColumns.UpdateColumnSet(
			claimAllColumns,
			claimPrimaryKeyColumns,
		)
		update = strmangle.SetComplement(update, claimColumnsWithAuto)

		if len(update) == 0 {
			return errors.New("models: unable to upsert claims, could not build update column list")
		}

		cache.query = buildUpsertQueryMSSQL(dialect, "claims", claimPrimaryKeyColumns, update, insert, ret)

		whitelist := make([]string, len(claimPrimaryKeyColumns))
		copy(whitelist, claimPrimaryKeyColumns)
		whitelist = append(whitelist, update...)
		whitelist = append(whitelist, insert...)

		cache.valueMapping, err = queries.BindMapping(claimType, claimMapping, whitelist)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(claimType, claimMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // MSSQL doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert claims")
	}

	if !cached {
		claimUpsertCacheMut.Lock()
		claimUpsertCache[key] = cache
		claimUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single Claim record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *Claim) Delete(exec boil.Executor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no Claim provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), claimPrimaryKeyMapping)
	sql := "DELETE FROM [dbo].[claims] WHERE [id]=$1"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from claims")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for claims")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q claimQuery) DeleteAll(exec boil.Executor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no claimQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from claims")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for claims")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o ClaimSlice) DeleteAll(exec boil.Executor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), claimPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM [dbo].[claims] WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, claimPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from claim slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for claims")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *Claim) Reload(exec boil.Executor) error {
	ret, err := FindClaim(exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *ClaimSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := ClaimSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), claimPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT [dbo].[claims].* FROM [dbo].[claims] WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, claimPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(nil, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in ClaimSlice")
	}

	*o = slice

	return nil
}

// ClaimExists checks if the Claim row exists.
func ClaimExists(exec boil.Executor, iD int64) (bool, error) {
	var exists bool
	sql := "select case when exists(select top(1) 1 from [dbo].[claims] where [id]=$1) then 1 else 0 end"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, iD)
	}

	row := exec.QueryRow(sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if claims exists")
	}

	return exists, nil
}
